# Only needed when frontend is deployed separately from backend.
# Set to your deployed backend URL (e.g. https://your-api.railway.app)
# Leave empty for local dev (Vite proxies /api to localhost:3001)
VITE_API_URL=http://localhost:8080
